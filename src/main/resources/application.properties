

# Database Configuration
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/notes_api_db}
spring.datasource.username=root
spring.datasource.password=Kiran@89
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
##spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration
#spring.jpa.hibernate.ddl-auto=update -- for local use
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

## JWT Configuration (we'll use these later)
jwt.secret=myVeryLongSecretKeyForJWTTokenGenerationThatIsSecureEnoughForHS512AlgorithmAndMeetsThe512BitRequirement
jwt.expiration=86400000

# Production settings
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update} 
server.port=${PORT:8080}